version: "3.9"

services:
  postgres:
    image: pgvector/pgvector:pg16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: agentlens
      POSTGRES_PASSWORD: agentlens
      POSTGRES_DB: agentlens
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agentlens"]
      interval: 5s
      timeout: 3s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  agentlens:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://agentlens:agentlens@postgres:5432/agentlens
      DATABASE_PATH: /app/data/agentlens.db
      REDIS_URL: redis://redis:6379
      STORAGE_BACKEND: postgres
      AUTH_DISABLED: "true"
      JWT_SECRET: dev-secret-agentlens-local
      ADMIN_API_KEY: dev-admin-key
      PORT: "3000"
    volumes:
      - agentkit-stack_agentlens-data:/app/data

volumes:
  pgdata:
  agentkit-stack_agentlens-data:
    external: true
